services:
  banco_poo:
    image: mysql:8.0
    container_name: mysql_poo
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: patasCloud_db
    ports:
      - "3307:3306"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
